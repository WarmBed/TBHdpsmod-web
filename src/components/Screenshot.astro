---
import { Image } from 'astro:assets';
interface Props { src: ImageMetadata; alt: string; className?: string; }
const { src, alt, className } = Astro.props;
---
<div class:list={['win', className]}><Image src={src} alt={alt} /></div>
